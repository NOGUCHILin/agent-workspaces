# Exclude only repo/ directories (actual git worktrees)
# CLAUDE.md, .claude/, docs/ are tracked for branch-specific settings
projects/*/worktrees/*/repo/
.mcp.json
!.mcp.json.example

# Personal projects (not shared)
workspaces/*/projects/

# Personal overrides (not shared)
*.local.md
*.local.json

# Playwright MCP work files
claude-workspace/.playwright-mcp/
